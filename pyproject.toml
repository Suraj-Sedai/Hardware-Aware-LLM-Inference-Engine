[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-inference-engine"
version = "0.1.0"
description = "Hardware-Aware LLM Inference Engine"
requires-python = ">=3.8"
