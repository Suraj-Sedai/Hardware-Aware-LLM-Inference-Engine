Make one generation path the source of truth.
Use InferenceController.generate() as the main path and stop benchmarking the old generate.py logic. Either delete the old path or rewrite src/inference/generate.py to use the same preallocated-buffer logic.
Fix the profiling API.
Either:
export get_device, get_gpu_utilization, get_cpu_utilization, and benchmark_generation from src/profiling/__init__.py, or
change all imports to from src.profiling.benchmark import ...
Right now the repo looks inconsistent.
Rewrite all experiments to use the controller + metrics module.
exp1, exp2, and exp3 are still old-style experiments. They should call the controller, get latencies, then call calculate_metrics(), and save structured results. Until you do that, your new profiling system is not actually being used.
Fix peak memory reporting.
Change controller output from ending memory to true peak memory using get_gpu_max_memory_mb().
Create one clean experiment result schema.
Every experiment should save the same core fields:
prompt_len, decode_len, batch_size, ttft_ms, tpot_avg_ms, tpot_p95_ms, throughput_tokens_per_sec, peak_memory_mb, phase_times.